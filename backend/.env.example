PORT=8000
APP_ENV=development

# Leave unset to use the local SQLite file (sqlite:///./members.db).
# On Render, set to the PostgreSQL connection string.
# DATABASE_URL=

# Leave unset to allow all CORS origins. Set to the deployed frontend URL
# in production.
# ALLOWED_ORIGIN=